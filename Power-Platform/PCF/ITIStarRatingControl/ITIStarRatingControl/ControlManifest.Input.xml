<?xml version="1.0" encoding="utf-8"?>
<manifest>
  <control namespace="ITI" constructor="ITIStarRatingControl" version="0.0.1"
    display-name-key="ITIStarRatingControl" description-key="ITIStarRatingControl description"
    control-type="standard">
    <!--external-service-usage
    node declares whether this 3rd party PCF control is using external service or not, if yes, this
    control will be considered as premium and please also add the external domain it is using.
    If it is not using any external service, please set the enabled="false" and DO NOT add any domain
    below. The "enabled" will be false by default.
    Example1:
      <external-service-usage enabled="true">
        <domain>www.Microsoft.com</domain>
      </external-service-usage>
    Example2:
      <external-service-usage enabled="false">
      </external-service-usage>
    -->
    <external-service-usage enabled="false">
      <!--UNCOMMENT
      TO ADD EXTERNAL DOMAINS
      <domain></domain>
      <domain></domain>
      -->
    </external-service-usage>
    <!-- property node identifies a specific, configurable piece of data that the control expects
    from CDS -->

    <!-- Binds to a Whole Number field on the Ticket entity -->
    <property name="rating"
              display-name-key="Rating Value"
              description-key="Whole number field to store the rating"
              of-type="Whole.None"
              usage="bound"
              required="true" />

    <!-- INPUT: Configuration properties — set by form designer, not end user -->
    <property name="maxStars"
              display-name-key="Max Stars"
              description-key="Number of stars to display (3 to 10). Default: 5"
              of-type="Whole.None"
              usage="input"
              required="false" />

    <property name="selectedColor"
              display-name-key="Selected Star Color"
              description-key="Hex color for filled stars e.g. #f59e0b"
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="unselectedColor"
              display-name-key="Unselected Star Color"
              description-key="Hex color for empty stars e.g. #d1d5db"
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <!--
      Property node's of-type attribute can be of-type-group attribute.
      Example:
      <type-group name="numbers">
        <type>Whole.None</type>
        <type>Currency</type>
        <type>FP</type>
        <type>Decimal</type>
      </type-group>
      <property name="sampleProperty" display-name-key="Property_Display_Key"
    description-key="Property_Desc_Key" of-type-group="numbers" usage="bound" required="true" />
    -->
    <resources>
      <code path="index.ts" order="1" />
      <css path="css/ITIStarRatingControl.css" order="1" />
      <!-- UNCOMMENT TO ADD MORE RESOURCES
      <resx path="strings/ITIStarRatingControl.1033.resx" version="1.0.0" />
      -->
    </resources>
    <!-- UNCOMMENT TO ENABLE THE SPECIFIED API
    <feature-usage>
      <uses-feature name="Device.captureAudio" required="true" />
      <uses-feature name="Device.captureImage" required="true" />
      <uses-feature name="Device.captureVideo" required="true" />
      <uses-feature name="Device.getBarcodeValue" required="true" />
      <uses-feature name="Device.getCurrentPosition" required="true" />
      <uses-feature name="Device.pickFile" required="true" />
      <uses-feature name="Utility" required="true" />
      <uses-feature name="WebAPI" required="true" />
    </feature-usage>
    -->
  </control>
</manifest>